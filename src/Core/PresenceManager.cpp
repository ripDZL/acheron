#include "PresenceManager.hpp"

// PresenceManager is fully defined inline in the header; this translation unit
// exists so AUTOMOC reliably processes the Q_OBJECT and the target links the
// generated meta-object code.
